language: python
python: '2.7'
sudo: required

branches:
  only:
  - centos7

services:
  - docker

before_install:
  - docker pull ilari/centos7-systemd

script:
 # Run tests in Container
 - docker run --privileged -v `pwd`:/WunderMachina:rw ilari/centos7-systemd /bin/bash -c "/WunderMachina/tests/test-in-docker.sh"

notifications:
  email: false
#  hipchat:
#    rooms: